import { createAction } from 'redux-act'

export const doSample = createAction('do sample')
export const fetchData = createAction('fetch data')
export const registData = createAction('regist data to store')
